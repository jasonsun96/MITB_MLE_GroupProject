R2_ACCOUNT_ID=R2_ACCOUNT_ID
R2_ACCESS_KEY_ID=R2_ACCESS_KEY_ID
R2_SECRET_ACCESS_KEY=R2_SECRET_ACCESS_KEY
RS_BUCKET=RS_BUCKET

# Optional. Set a token to password-protect the local Jupyter server.
# Leave empty for no token (only safe because the port is bound to 127.0.0.1).
JUPYTER_TOKEN=
